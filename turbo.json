{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "outputLogs": "new-only"
    },
    "schema": {
      "outputs": ["options*"],
      "outputLogs": "new-only",
      "inputs": ["core/**/typed.ts"]
    },
    "test": {}
  }
}
